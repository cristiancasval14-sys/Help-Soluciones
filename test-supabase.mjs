import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'https://oanveqlukrqpjrahwxjj.supabase.co';
const supabaseKey = 'sb_publishable_5ceYHGqFvodwLADgKI6PGg_7M4TQFyp';

const supabase = createClient(supabaseUrl, supabaseKey);

async function testInsert() {
    console.log("Testing Supabase Insert...");

    // UUID will hopefully be generated by default
    const { data: profile, error } = await supabase.from('profiles').insert([{
        username: 'testu_random',
        password: '123'
    }]).select();

    if (error) {
        console.error("Error inserting profile:", error);
    } else {
        console.log("Successfully inserted profile:", profile);
    }
}

testInsert();
